# Custom patches (TRT Digital fork of whatomate)

This fork (`naimbic/trtWhats`) diverges from upstream `shridarpatil/whatomate`.
**Before merging/rebasing upstream, re-apply or verify every item below.** Each
code patch is marked in-source with a greppable tag so conflicts are easy to spot:

```
grep -rn "TRT custom patch" internal/ frontend/src/
```

| # | Area | File(s) | What / Why | Commit |
|---|------|---------|------------|--------|
| 42 | Propagate account rename to conversations | `internal/handlers/accounts.go` (`UpdateAccount`) | An account is referenced everywhere by its NAME (`messages.whats_app_account`, `contacts.whats_app_account`, and per #21 flows/keywords/templates). Renaming an account orphaned every historical conversation — the #41 number chips filter by the current name, so after a rename they matched nothing (looked like "filter broken again"). Now when `UpdateAccount` changes the name, it relabels the org's `messages` and `contacts` rows from the old name to the new one (`WHERE organization_id = ? AND whats_app_account = <old>`), a pure relabel using `idx_messages_account`. Keeps the name-based model consistent so the chips (and any name-coupled feature) keep working across renames. **Accounts renamed BEFORE this shipped** are already orphaned; reconcile once via the UI (rename back to the original name, Save, then to the new name, Save — which fires this propagation). | _this change_ |
| 41 | Number chips + per-number unread counts | `internal/handlers/contacts.go` (`ListContacts` account filter, `AccountUnreadCounts`) + `cmd/whatomate/main.go` (route) + `frontend` (`services/api.ts`, `stores/contacts.ts`, `services/websocket.ts`, `views/chat/ChatView.vue`, 4 locale files) | In a space with more than one WhatsApp number, the number a message came in on was only visible **after** opening the chat. Now a row of **number chips** sits above the conversation list (only when the space has >1 account): an **All** chip plus one per number, each with a green bubble showing that line's **unread-message count**. Clicking a number filters the inbox to just its conversations (`GET /api/contacts?account=<name>`); All clears it. Filtering and counting are **message-based**, not contact-based: `contacts.whatsapp_account` is never populated for inbound-created contacts (and one contact can message several of the space's numbers — hence the in-session tabs), so a conversation belongs to a number when it has a message on it (`EXISTS` on `messages.whatsapp_account`). Counts come from `GET /api/contacts/account-unreads` — unread incoming messages grouped by the number they arrived on (`messages.whatsapp_account`), org-scoped (a space only ever sees its own numbers) and honoring the same assigned-contact scoping as the list. Counts refresh on mount, on each inbound WebSocket message, and when opening a chat marks it read. Everything is per-space by construction (org-scoped), so the trtDigital space shows its 4 numbers and never another space's. | _this change_ |
| 40 | Conversion value/qty persists on reopen | `frontend/src/stores/contacts.ts` (`applyConversionUpdate`) + `frontend/src/components/chat/ContactInfoPanel.vue` (`saveConversion`) | The agent-entered Quantity/Conversion value (#36) saved to the DB but looked like it "didn't save": `selectContact` reuses the cached contact object from the loaded list (no refetch), and `saveConversion` never updated it — so reopening the contact re-read the stale cached value (`0`) and the input `watch` reset the fields to 0. Now a successful save patches quantity/value/sent-at back into **both** the list entry and `currentContact` (mirrors `updateContactTags`), so the values survive closing/reopening with no refresh. | _this change_ |
| 38 | Real-time open-chat refresh + CTWA referral diagnostics | `frontend/src/services/websocket.ts` (`refreshStaleData`) + `internal/handlers/webhook.go` (raw inbound logging) + `internal/handlers/meta_conversions.go` (`captureCTWAReferral` diagnostic log) | (a) Messages arriving while the chat was open didn't show until a manual refresh — `refreshStaleData` on WS reconnect now also refetches the **open** conversation's messages. (b) All ad-sourced contacts showed "Not from a Click-to-WhatsApp ad": added diagnostic logging of raw inbound message webhooks (WITH/WITHOUT `referral`, plus sender + type) which confirmed Meta is **not** attaching `ctwa_clid` to the inbound messages — an ad/number/app link issue on Meta's side, not app code. | _this change_ |
| 37 | Fix account save corrupting WhatsApp token | `internal/handlers/accounts.go` (`UpdateAccount`, `encryptAccountSecrets` incl. `MetaAccessToken`) + `internal/handlers/cache.go` (`MetaAccessToken` in `whatsAppAccountCache`) | Saving a space's settings corrupted its WhatsApp access token to plaintext → every send failed with `API error 190: Authentication Error`. Cause: `resolveWhatsAppAccountByID` returns the account with secrets **decrypted**, and `UpdateAccount` saved it back without re-encrypting. Now `encryptAccountSecrets` runs before `DB.Save` (skips values already `enc:`-prefixed), covering both the WhatsApp token and the per-space Meta token. Verified locally: token stays `enc:` after save. | _this change_ |
| 36 | Agent-confirmed conversion value + quantity | `internal/models/models.go` (contact fields) + `internal/handlers/contacts.go` (`SetContactConversion`) + `internal/handlers/chatbot_processor.go` (remove auto-send, seed value) + `internal/handlers/meta_conversions.go` (returns bool, `num_items`) + `cmd/whatomate/main.go` (route) + `frontend` (`ContactInfoPanel.vue`, `api.ts`, `stores/contacts.ts`) | So Meta gets the **correct order value**: beside the Converted tag the agent now sets **Quantity** + **Conversion value**, and **saving sends the Meta conversion** (this replaces the auto-send on form submission from #35). New contact columns `conversion_quantity`, `conversion_value`, `meta_conversion_sent_at`. `PUT /api/contacts/{id}/conversion` (gated by `tags:write`, like tagging) stores the values and, on the first save with a value, resolves the contact's space, decrypts its token, and posts the Purchase event with `custom_data.value` + `num_items=quantity`; `sendMetaConversion` now returns whether it actually posted, and `meta_conversion_sent_at` is set only then (so a rejected send can be retried, and a successful one is never double-counted — the fields lock in the UI once sent). The order form still auto-tags Converted and seeds a suggested value if it carried one. Verified locally end-to-end (real authenticated request): order → Converted with **no** auto-send → agent saves 2×250 → payload built with `value:250, num_items:2` + ctwa_clid + hashed phone, saved to DB. | _this change_ |
| 35 | Meta offline conversions (free, per-space) | `internal/handlers/meta_conversions.go` (new) + `internal/handlers/chatbot_processor.go` (`Referral` field, `captureCTWAReferral`, `tagContactConverted` returns bool + send hook) + `internal/models/models.go` (per-space account fields) + `internal/handlers/accounts.go` (request/response/create/update) + `frontend/src/views/settings/AccountDetailView.vue` (settings card) + `internal/config/config.go` (`MetaConfig` = token + defaults) + `config.example.toml` + `.env.coolify.example` | Send "order placed" clients to Meta as conversions — **free**, direct POST to `graph.facebook.com` (no gateway, no per-event cost). On every inbound message `captureCTWAReferral` stores the Click-to-WhatsApp click id (`ctwa_clid`) + ad source on the contact metadata. On the FIRST Converted tag (order form submitted — `tagContactConverted` now returns whether newly tagged), `sendMetaConversion` posts a `Purchase` event with `action_source:"business_messaging"`, `messaging_channel:"whatsapp"`, `user_data:{ ctwa_clid, ph:<sha256 phone> }`, `custom_data:{ value, currency }`, `event_id` for dedup. **Config is per-space so multi-client setups work:** **enable / dataset id / access token / test-event-code / currency / default-value are all PER WhatsApp account** (new columns on `whatsapp_accounts`, edited in the account's settings card), so each number/client reports to its own ad account with its own token. The per-space token is encrypted at rest (added to `DecryptSecrets`/`encryptAccountSecrets` **and** carried explicitly through the Redis `whatsAppAccountCache` alongside the other `json:"-"` secrets — otherwise it would silently vanish through the cache). `WHATOMATE_META__ACCESS_TOKEN` remains only an OPTIONAL global fallback used when a space has no token of its own. Value comes from the order form (`total`/`amount`/…) else the space's default else the global default. No-op unless the space is enabled with a dataset AND the global token is set; token never logged; async; one send per contact. Verified locally end-to-end (mock Graph): CTWA referral captured, per-space `dataset=SPACE_DS_777` used with the global token, value fell back to the space default, hashed phone matches, dedup confirmed; settings card loads/saves the per-space values. The app's SSRF guard blocks loopback (prod uses the public Graph host). | _this change_ |
| 34 | Languages (EN/FR/AR/Darija) + RTL | `frontend/src/i18n/index.ts` + `frontend/src/i18n/locales/` (add `fr.json` + `ary.json`, remove `es/hi/ta.json`, backfill `ar.json`) + `frontend/src/App.vue` + `frontend/src/components/layout/AppLayout.vue` + `frontend/src/views/chat/ChatView.vue` | Limit the UI to **English, French, Arabic, and Moroccan Darija** (deleted es/hi/ta; added full `fr.json` and `ary.json` — Darija in Arabic script, code `ary`; trimmed the locale-name map). Locales are auto-discovered from `locales/*.json`, so the switcher lists exactly these four. Backfilled `ar.json` with the newer keys it was missing (the #31 date filter, #33 read/unread filter, media-viewer, file-size messages) so nothing falls back to English in Arabic; `fr.json`/`ary.json` were translated from the current `en.json` so they already have them. The two chat "Clear" buttons now use `common.clear` instead of hard-coded English. **RTL:** `applyDir()` sets `<html dir="rtl">` for Arabic **and Darija** (both Arabic-script) on load and on every `setLocale`; `App.vue` wraps everything in reka-ui's `ConfigProvider :dir` bound to the locale so ScrollArea/Popover/dropdowns become direction-aware (without it reka's ScrollArea forced `direction: ltr`, so chat bubbles didn't flip). The desktop sidebar and the chat contact-list are flex children, so they mirror to the **right** automatically under RTL; structural dividers use logical `border-e`; the mobile drawer slides from the right. Verified live: Arabic & Darija → menu + conversation list on the right, incoming bubbles on the right, filters translated; English/French → normal LTR, no regression. | _this change_ |
| 33 | Read / unread filter | `internal/handlers/contacts.go` (`ListContacts`) + `frontend/src/services/api.ts` + `frontend/src/stores/contacts.ts` (`readFilter`, fetch race guard) + `frontend/src/views/chat/ChatView.vue` + `frontend/src/i18n/locales/en.json` | Filter the conversation list by read status. Backend accepts a `read` query arg: `read=false` → only conversations with unread (new) incoming messages (`EXISTS` incoming message where `status <> 'read'` — same rule as the unread badge), `read=true` → only fully-read ones. Frontend adds an envelope dropdown (All / Unread / Read) next to the tag + date filters, with a removable active badge; the store carries `readFilter` and threads it through both `fetchContacts` and `loadMoreContacts`. Also hardened `fetchContacts` with a request-sequence guard so rapidly switching filters can't apply an out-of-order (stale) response — this also protects the tag/date/search filters. Verified live and via direct API: All→8, Unread→3, Read→5. | _this change_ |
| 32 | No list reload on each message | `frontend/src/services/websocket.ts` (`handleNewMessage`) + `frontend/src/stores/contacts.ts` (`applyRealtimeContactUpdate`, `markContactReadLocal`, `addMessage`) | The inbox flickered/jumped and lost its state whenever messages arrived: every WebSocket message called `store.fetchContacts()`, which replaced the whole sidebar array (page 1), so with Belle Tulipe's ~2000 msgs/day the list constantly rebuilt — scroll jumped, loaded pagination reset, and the active date/tag filter was dropped. Now a realtime message updates just the affected contact **in place** (`applyRealtimeContactUpdate`: bumps `last_message_at` so the `sortedContacts` computed reorders only that one row, sets unread/last_inbound), and only falls back to a fetch when the contact isn't already in the current (possibly filtered) list — e.g. a brand-new conversation. `addMessage` no longer bumps the unread badge for the conversation that's open; after `markRead` succeeds the badge is cleared locally instead of refetching. Verified end-to-end locally (real webhook → WS broadcast): two inbound messages reordered contacts to the top with unread badges and **zero** `/api/contacts` refetches. | _this change_ |
| 31 | Conversations date filter | `internal/handlers/contacts.go` (`ListContacts`) + `frontend/src/services/api.ts` + `frontend/src/stores/contacts.ts` + `frontend/src/views/chat/ChatView.vue` + `frontend/src/i18n/locales/en.json` | Filter the conversation list by **last activity** window. Backend accepts `from`/`to` (RFC3339) query args and adds `last_message_at >= / <=` WHERE clauses. Frontend adds a calendar dropdown in the inbox header (Today / Yesterday / Last 7 days / Last 30 days / Custom range) alongside the tag filter; the store carries `dateFrom`/`dateTo` and passes them through both `fetchContacts` and `loadMoreContacts` (pagination preserves the filter). An active filter shows a removable badge. | _this change_ |
| 1 | Config | `internal/config/config.go` | Parse `DATABASE_URL` / `REDIS_URL` connection strings (managed Postgres/Redis on Coolify). Adds `applyDatabaseURL`/`applyRedisURL`. | a7df019 |
| 2 | Redis TLS | `internal/config/config.go`, `internal/database/redis.go` | `RedisConfig.TLSSkipVerify` + `redis://` disables TLS, `?tls_skip_verify=true` for self-signed certs. | c28f2b0 |
| 3 | Deploy | root `Dockerfile`, `docker-compose.coolify.yaml`, `.env.coolify.example`, `DEPLOY-COOLIFY.md`, `.dockerignore` | Root Dockerfile (multi-stage + ffmpeg/Piper TTS) so Coolify's default build pack finds it; Coolify deploy assets. | 61299c6, 91dc551, 1519318 |
| 4 | Calling | `internal/handlers/outgoing_calls.go` | Decrypt account access token (`decryptAccountSecrets`) before `ToWAAccount()` for calling APIs — else encrypted token → Meta error 190. | d647c25 |
| 5 | Media / multi-org | `internal/handlers/media.go` (`ServeMedia`) **+** `frontend/src/components/layout/OrganizationSwitcher.vue` | **Backend (robust fix):** for super admins, `ServeMedia` resolves the message by its UUID across any org (element `<img>`/`<video>` requests can't carry `X-Organization-ID`, so the cookie's org may not match the message's org → org-scoped lookup 404s → inbound images silently hidden). Non-super users stay org-scoped. **Frontend (defense in depth):** super-admin org switch also re-issues the session cookie (`switchOrg`), not just the header, with header-only fallback. | _this change_ |
| 6 | Notification sound | `frontend/src/services/websocket.ts` (`playNotificationSound`) | Synthesize a WhatsApp-style two-note "ding" via Web Audio API instead of loading `/notification.mp3` (no copyrighted asset). Autoplay policy still requires one user interaction. | _this change_ |
| 7 | Unread badge | `frontend/src/views/chat/ChatView.vue` (conversation list) | WhatsApp-style solid-green (`#25D366`) circular unread-count bubble, white text, `99+` cap; hidden via `v-if` once the chat is opened (`unread_count` resets to 0). Restyle only — the count logic already existed. | _this change_ |
| 8 | Unread badge (server) | `internal/handlers/chatbot_processor.go` (`saveIncomingMessage`) | Stop pre-marking bot-handled inbound messages as `read` (upstream did this for issue #280 to avoid a badge flash). Leaving them `Received` makes the WhatsApp-style unread count (contacts.go: `status != read`) reflect real inbound messages, so the green bubble shows per contact and clears only when an agent opens the chat. Pairs with patch #7 (badge styling). | _this change_ |
| 9 | Media re-download resilience | `internal/models/models.go` (`Message.MediaID`) + `internal/handlers/chatbot_processor.go` (`MediaInfo`, extraction, `saveIncomingMessage`) | Persist the Meta `media_id` on each inbound media message so a lost/wiped local file can be re-fetched from Meta (within its ~30d retention). New nullable column `media_id` (added by GORM auto-migrate on `-migrate`). Does not restore media saved before this field. **Also requires** a persistent volume on the media dir (`/app/uploads`) — this is the belt to that suspenders. | _this change_ |
| 10 | Incoming-call ring | `frontend/src/stores/calling.ts` (`handleCallEvent`) | Synthesize a repeating ring tone (Web Audio) while a `call_transfer_waiting` is pending; stop on connected/completed/abandoned/no_answer. whatomate had no incoming-call ring sound. Autoplay policy applies (one prior click). | _this change_ |
| 11 | Call-permission Darija | `pkg/whatsapp/call.go` (`SendCallPermissionRequest`) | Default call-permission-request body text changed from English to Moroccan Darija. | _this change_ |

| 12 | Team member multi-org | `internal/handlers/teams.go` (`AddTeamMember`) | Allow adding a user to a team if they're a **member** of the team's org (home org OR a `user_organizations` row), not only if it's their *home* org. Upstream rejected multi-org members (who can switch into the org) with a misleading "User not found". | _this change_ |

| 13 | Reply to media-only messages | `internal/handlers/chatbot_processor.go` (`processIncomingMessage`) | Upstream drops messages with no text, so a client who sends only a photo/screenshot/voice note got no reply (conversation stalls, bot re-asks on later texts). Now media messages (image/video/document/sticker/audio) send `FallbackMessage` (acknowledge + ask size/colour/city / staff will contact), debounced 90s via `chatbot_last_message_at` to avoid burst spam. | _this change_ |

| 14 | Auto-tag Lost (5d) | `internal/handlers/sla_processor.go` (`processOrganizationSLA` + `tagLostContacts`) | Periodic job tags contacts "Lost" (`ضائع - Perdu`) after 5 days with no inbound reply, unless already Converted/Lost. Runs for SLA-enabled orgs. Tag names hardcoded — update constants if tags are renamed. | _this change_ |

| 15 | Fallback debounce | `internal/handlers/chatbot_processor.go` (`processIncomingMessage`) | Only send the unmatched-text `FallbackMessage` if the bot hasn't messaged the contact in the last 15 min — stops the same fallback firing on every unrecognised word (spammy when AI is off). Real fix is a valid AI key. | _this change_ |

| 16 | Gemini model list | `frontend/src/views/settings/ChatbotSettingsView.vue` | AI-model dropdown refreshed to current Gemini models (gemini-3.6-flash, gemini-2.5-flash/pro/flash-lite); the old gemini-2.0/1.5 entries 404 on generateContent (deprecated). | _this change_ |

| 30 | Log message-failure reason | `internal/handlers/webhook.go` (`updateMessageStatus`) | Meta's status webhook carries the failure code/reason, which was stored on the message but not logged — a `status=failed` gave no clue why. Now a failed delivery logs `Message delivery failed` with `error_code` + `error_reason` (e.g. 131047 = outside 24h window), so failures are diagnosable from the logs. | _this change_ |

| 29 | Tag contacts via tags perm | `internal/handlers/contacts.go` (`UpdateContactTags`) + `frontend/src/components/chat/ContactInfoPanel.vue` (`canEditTags`) | Applying a tag to a contact was gated by `contacts:write` (both backend and the UI button), so granting agents the Tags permission never let them tag — agents only have `contacts:read`. Now gated by `tags:write`, so an agent role with tags:write can apply Converted/Lost/Waiting without full contact-edit rights. | _this change_ |

| 28 | Voice-note recording | `internal/handlers/contacts.go` (`SendMediaMessage`, `transcodeAudioToOgg`), `pkg/whatsapp/client.go` (`UploadMedia`), `frontend/src/views/chat/ChatView.vue` (mic button + MediaRecorder) | Agents record + send a voice note from the composer (tap mic, timer, send/cancel). Backend transcodes the browser recording (Chrome=webm) to canonical ogg/opus via **ffmpeg decode→wav then `opusenc`** (opus-tools) — ffmpeg's own libopus ogg was valid per ffprobe but Meta processed it as octet-stream (131053); opusenc's reference output works. `UploadMedia` rebuilt with std `mime/multipart` + explicit file-part Content-Type + a `type` form field (the hand-rolled body left the part as octet-stream, which is what Meta stored); it also GETs the media back to log `stored_mime`. Meta wants bare `audio/ogg` for upload. | _this change_ |

| 27 | Cache TTL 6h → 10min | `internal/handlers/cache.go` | All server-side caches (settings, flows, keywords, WhatsApp accounts, webhooks, SLA, AI contexts, user/role permissions, tags) had a 6-hour TTL, so any settings/permission edit whose handler forgot to invalidate (or a direct DB change) stayed stale for up to 6h — edits "didn't take effect". TTLs cut to 10 minutes as a safety net so every change applies within 10 minutes at most, regardless of invalidation. | _this change_ |

| 26 | Out-of-hours debounce | `internal/handlers/chatbot_processor.go` (`sendOutOfHoursOnce`) | The built-in out-of-hours message fired on every inbound after closing (spam). Now it is sent only if it was not already the last message to that contact within 8h — so a client messaging several times after hours gets one "we'll contact you tomorrow" reply, not many. Used by both out-of-hours branches. | _this change_ |

| 25 | Agent takeover pauses bot | `internal/handlers/contacts.go` (`SendMessage`, `ensureAgentTakeoverTransfer`) | When a human agent sends a message in a conversation, the chatbot/AI now stops replying to that contact — it creates an active agent transfer (if none), which `hasActiveAgentTransfer` uses to skip the bot in `processIncomingMessageFull`. The existing **Resume** button on the transfer hands the contact back to the bot. Previously the AI kept replying alongside the agent unless a flow/keyword transfer had fired. | _this change_ |

| 24 | Flow photo → public link | `internal/handlers/chatbot_processor.go` (`resolveFlowMedia`, `formatMediaID`, nfm_reply `UseNumber`), `internal/handlers/media.go` (`ServeFlowMedia`), `cmd/whatomate/main.go` (public route + skip-auth), `internal/config/config.go` (`App.PublicURL`) | A WhatsApp Flow PhotoPicker returns only a media **id**, so order photos never reached the Sheet. On submission whatomate downloads the picker media (account token, same path as inbound media), saves it under `flow-media/` with a random UUID name, and injects a **public URL** `<field>_url` (+ `_mime`/`_filename`) into the flow response. Served unauthenticated at `/media/flow/{name}` so it renders in a Google Sheet (`=IMAGE()`) / email — no n8n download or Drive step needed. Flow JSON parsed with `UseNumber` for exact media ids. `App.PublicURL` (env `WHATOMATE_APP__PUBLIC_URL`, default `https://trtwhats.3a.ma`) builds the absolute link. **Needs the `/app/uploads` (media) dir on a persistent volume** or links break on redeploy. | _this change_ |

| 23 | `{{json:}}` template directive | `internal/handlers/template_engine.go` (`processVariables`) | New `{{json:var}}` directive emits a value as valid JSON. Needed so a webhook/API-node body can forward complex values (e.g. a WhatsApp Flow PhotoPicker photo array, which carries the base64 image) — plain `{{var}}` uses `fmt %v` and produces invalid JSON. The Belle Tulipe order webhook now sends `"modele_image":{{json:modele_image}}` so the uploaded model photo reaches n8n. | _this change_ |

| 22 | Auto-Converted on order | `internal/handlers/chatbot_processor.go` (`processIncomingMessageFull`, `tagContactConverted`) | When a customer submits an order form (WhatsApp Flow `nfm_reply`), auto-tag them "تم البيع - Converti" (Converted) — idempotent. Surfaces them on the dashboard and excludes them from the 5-day Lost auto-tagging (#14). | _this change_ |

| 21 | Flow account rebind | `internal/handlers/flows.go` (`UpdateFlow`) | Flows resolve their WhatsApp account by **exact name**, so renaming an account (e.g. "Belle Tulipe 1430" → "Belle Tulipe inezgane 1430") leaves every flow bound to the old name → Save-to-Meta/Publish fail with "WhatsApp account not found". Upstream's `UpdateFlow` never persisted `whatsapp_account`, so the binding couldn't be fixed from UI **or** API. Now a PUT with `whatsapp_account` rebinds the flow to the current account name. | _this change_ |

| 20 | Rebrand to trtWhats | `frontend/src/components/icons/WhatsAppIcon.vue` (new) + `frontend/src/components/layout/AppLayout.vue` + `frontend/index.html` + `frontend/public/favicon.svg` | Renamed the product from "Whatomate" to **trtWhats** and swapped the generic `MessageSquare` logo mark for a proper WhatsApp glyph (new `WhatsAppIcon.vue`; lucide dropped brand icons). Updated the sidebar + mobile-header logo, the page `<title>`/description, and the favicon (green WhatsApp tile). | _this change_ |

| 19 | Multimodal AI (see images / hear voice notes) | `internal/handlers/chatbot_processor.go` (`AIMedia`, `collectAIMedia`, `generateAIResponse`, `generateGoogleResponse`, `processIncomingMessageFull`) + `internal/handlers/chatbot_graph_runner.go` (pass `nil`) | The AI path only ever sent **text** to Gemini, so the bot couldn't see photos or hear voice notes (it re-asked for images already sent, ignored audio). Now inbound image/audio/sticker media (already downloaded to disk) is read, base64-encoded, and attached to the Gemini request as `inline_data` parts — for both a captioned photo and a media-only message (which previously hit an early `return` with a canned fallback). Gemini reads OGG/Opus voice notes and images natively; mime is stripped of `; codecs=…`. Matches what Meta's native WhatsApp Business Agent does. Media-only messages now get a real AI reply (falls back to the debounced canned line only if AI is off/errors). Only image+audio sent (not video/docs) to keep requests small. | _this change_ |

| 18 | KPI sparklines | `internal/handlers/widgets.go` (widget data builder) **+** `frontend/src/views/dashboard/DashboardView.vue` | **Backend:** number/percentage widgets now also return a daily `chart_data` series (reuses `getChartData`, respects the widget's filters). **Frontend:** number KPI cards render a mini sparkline (filled Chart.js line, no axes/legend/tooltip) under the value, coloured by the widget colour — matches the reference dashboard's KPI cards. Shown only when >1 data point. | _this change_ |

| 17 | Widget tags reporting + external link | `internal/handlers/widgets.go` (`widgetDataSources`, `allowedFilterFields`, `buildFilterSQL`, `getGroupedData`) **+** `frontend/src/views/dashboard/DashboardView.vue` | **Backend:** expose the contacts `tags` JSONB column to the dashboard widget builder so tag-based widgets work (Converted / Lost / Waiting reporting). `tags` is a JSONB array, so a scalar `=`/`ILIKE` never matches and grouping by the raw column buckets by the whole array — special-cased: filters use the containment operator `tags @> ?::jsonb` (GIN-indexed), group_by unnests via `jsonb_array_elements_text(tags)`. Enables a "Converted clients" count widget + a status breakdown pie/bar chart. **Frontend:** number and chart widget cards render an optional external link when `config.link_url` is set (`config.link_label` for the text) — whatomate's only link widget ("shortcuts") is limited to internal RouterLinks from a fixed registry, so there was no way to surface a Google Sheet / external URL on the dashboard. | _this change_ |

## Notes for maintainers
- **No `upstream` remote is configured.** To pull upstream safely:
  `git remote add upstream https://github.com/shridarpatil/whatomate.git`
  then `git fetch upstream && git merge upstream/main` (resolve conflicts, re-check the table above).
- Custom edits live as normal commits on `main` (the branch Coolify builds from).
- The tag `TRT custom patch` in source comments marks intentional divergences — never delete one during a conflict resolution without re-implementing it.
- Deeper root cause for #5: `internal/handlers/app.go` resolves org from `X-Organization-ID` (header) with cookie/JWT fallback; media handlers (`ServeMedia`) run through element requests that carry only the cookie. A fuller fix would let media requests carry the active org (e.g. signed URL or org in path), but the cookie re-issue above is sufficient and low-risk.
